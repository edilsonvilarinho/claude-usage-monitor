{
  "name": "@claude-usage/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Tipos e schemas Zod compartilhados entre Electron e server",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "zod": "4.3.6"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
